# # This is a basic workflow to help you get started with Actions

# name: GeoNode Client 5.0.x CI (build)

# concurrency: 
#   group: "client_build_5.0.x"
#   cancel-in-progress: true
# # Controls when the workflow will run
# on:
#   # Triggers the workflow on push events but only for the 5.0.x branch
#   push:
#     branches: [ 5.0.x ]
#     paths: 
#       - "VERSION"
#       - "geonode_mapstore_client/client/**"
#       - "!geonode_mapstore_client/client/version.txt"

# on:
#   # Trigger on every push to an open PR
#   pull_request:
#     types: [opened, synchronize, reopened]
#     paths:
#       - "VERSION"
#       - "geonode_mapstore_client/client/**"
#       - "!geonode_mapstore_client/client/version.txt"

#   # Also trigger on direct pushes to main (e.g. after PR merge)
#   push:
#     branches:
#       - main
#     paths:
#       - "VERSION"
#       - "geonode_mapstore_client/client/**"
#       - "!geonode_mapstore_client/client/version.txt"

#   # Allows you to run this workflow manually from the Actions tab
#   workflow_dispatch:

# # A workflow run is made up of one or more jobs that can run sequentially or in parallel
# jobs:
#   build_and_commit:
#     permissions:
#       contents: write
#     env:
#       working-directory: ./geonode_mapstore_client/client
#     runs-on: ubuntu-latest
#     steps:
#       - name: "Checking out"
#         uses: actions/checkout@v4
#         with:
#           submodules: "recursive"
#           # For PRs, check out the head branch so we can push back to it
#           ref: ${{ github.event.pull_request.head.ref || github.ref }}

#       ###########
#       # SET UP
#       #########
#       - name: "Setting up npm"
#         uses: actions/setup-node@v4
#         with:
#             node-version: 20

#       ############
#       # CACHING
#       ##########
#       - name: "Cache node modules"
#         uses: actions/cache@v4
#         env:
#           cache-name: cache-node-modules
#         with:
#           path: ~/.npm # npm cache files are stored in `~/.npm` on Linux/macOS
#           key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/package.json') }}
#           restore-keys: |
#             ${{ runner.os }}-build-${{ env.cache-name }}-
#             ${{ runner.os }}-build-
#             ${{ runner.os }}-

      ###############
      # COMMIT
      #############
      # - name: "Stage files"
      #   run: git add --all
        
      # - name: "Create Pull Request"
      #   uses: peter-evans/create-pull-request@v6
      #   with:
      #     token: ${{ steps.generate-token.outputs.token }}
      #     base: "5.0.x"
      #     title: "[5.0.x] new client build"
      #     body: "Automated client build from GeoNode Client 5.0.x CI"
      #     branch: "5.0.x-create-pull-request/patch"
